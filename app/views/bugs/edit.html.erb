<div class="min-h-screen bg-canvas pb-12 px-4" style="padding-top: 90px;">
  <div class="max-w-2xl mx-auto">
    <div class="mb-4">
      <%= link_to bug_path(@bug), class: "text-fz-blue-light hover:text-fz-blue-dark font-semibold inline-flex items-center gap-2", data: { controller: "keyboard-shortcut", keyboard_shortcut_key_value: "b" } do %>
        &larr; <%= t('bugs.edit.back') %>
        <span class="bg-white/20 text-fz-blue-light text-xs font-bold px-1.5 py-0.5 rounded">B</span>
      <% end %>
    </div>

    <div class="bg-surface rounded-xl shadow-lg p-8">
      <h1 class="text-3xl font-bold text-ink-darkest mb-6"><%= t('bugs.edit.title') %></h1>

      <%= render 'form', bug: @bug %>

      <div class="mt-6 pt-6 border-t border-ink-lighter">
        <%= button_to @bug, method: :delete, data: { turbo_confirm: t('bugs.edit.confirm_delete') }, class: "text-fz-red-dark hover:text-fz-red-light font-semibold bg-transparent border-0 cursor-pointer" do %>
          &#128465;&#65039; <%= t('bugs.edit.delete_bug') %>
        <% end %>
      </div>
    </div>
  </div>
</div>
