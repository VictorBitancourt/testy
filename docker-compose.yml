services:
  testy:
    image: ghcr.io/victorbitancourt/testy:latest
    ports:
      - "3000:80"
    environment:
      SOLID_QUEUE_IN_PUMA: "true"
    volumes:
      - testy_storage:/rails/storage
    restart: unless-stopped

volumes:
  testy_storage:
